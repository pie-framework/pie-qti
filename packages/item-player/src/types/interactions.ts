/**
 * Compatibility barrel for interaction data types.
 *
 * Canonical interaction modules live under `../interactions`. This path remains
 * for existing imports from `@pie-qti/item-player` and internal consumers.
 */

export type * from '../interactions/index.js';
