/**
 * Compatibility barrel for standard QTI extractors.
 *
 * New code should import from `../../interactions/index.js` or the specific
 * `../../interactions/<interaction>/index.js` module. This path remains for
 * existing tests and public consumers.
 */

export * from '../../interactions/index.js';
