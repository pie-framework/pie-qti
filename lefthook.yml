pre-commit:
  parallel: true
  commands:
    svelte-build:
      glob: "{apps,packages}/**/*.{js,ts,svelte,css}"
      run: bun run --cwd apps/demo build && bun run --cwd apps/transform build && bun run --cwd apps/docs build
