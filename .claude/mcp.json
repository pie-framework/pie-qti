{
  "mcpServers": {
    "imagen": {
      "command": "bun",
      "args": ["run", ".mcp-imagen/src/index.ts"],
      "env": {
        "GOOGLE_API_KEY": "${GOOGLE_API_KEY}"
      },
      "description": "Google Imagen 4 image generation MCP server"
    }
  }
}
